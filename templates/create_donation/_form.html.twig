{{ form_errors(form) }}

{{ form_start(form) }}
<div class="row">

    <div class="col-12">
        {{ form_row(form.amount) }}
    </div>

    <div class="col-12">
        {{ form_row(form.anonymous) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.payment.paymentMethod) }}
    </div>


    <div class="col-md-6">
        {{ form_row(form.payment.phoneNumber) }}
    </div>

    <div class="col-12">
        {{ form_row(form.donatorFullName) }}
    </div>

    <div class="col-12w">
        {{ form_row(form.comment) }}
    </div>

    <div class="col-12w">
        {{ form_row(form.agreeTerms) }}
    </div>

    <div class="d-flex justify-content-end">
        <button type="submit" class="btn btn-primary">Participer</button>
    </div>
</div>
{{ form_end(form) }}